<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.shopping.mall.user.mapper.UserMapper">
    <select id="findByEmail" parameterType="string" resultType="com.shopping.mall.user.entity.User">

        SELECT
        id,
        email,
        password,
        status
        FROM user
        WHERE email = #{email}

    </select>

    <insert id="saveUser">

        INSERT INTO user
        (
        email,
        password,
        status
        )
        VALUES
        (
        #{email},
        #{password},
        'ACTIVE'
        )

    </insert>

    <select id="findUserRole" parameterType="long" resultType="string">

        SELECT r.role_name
        FROM user_role ur
        JOIN role r
        ON ur.role_id = r.id
        WHERE ur.user_id = #{userId}
        LIMIT 1

    </select>


</mapper>